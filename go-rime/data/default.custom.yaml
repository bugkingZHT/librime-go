patch:
  schema_list:
    - schema: luna_pinyin_simp  # 朙月拼音·简化字

  # 候选词数量
  menu/page_size: 9
  
  # ASCII 模式
  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Shift_L: inline_ascii
      Shift_R: commit_text
      Control_L: noop
      Control_R: noop
      Caps_Lock: clear
      Eisu_toggle: clear
